import { RepoDirective } from './repo.directive';

describe('RepoDirective', () => {
  it('should create an instance', () => {
    const directive = new RepoDirective();
    expect(directive).toBeTruthy();
  });
});
